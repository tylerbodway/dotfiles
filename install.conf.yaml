- defaults:
    link:
      create: true
      relink: true

- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- link: {}

- shell:
  - [git submodule update --init --recursive, Installing submodules]