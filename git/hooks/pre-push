#!/usr/bin/env bash
set -e

# Delegation shim: forwards to repo-local pre-push hook if it exists.
# Required because core.hooksPath overrides per-repo .git/hooks/.

LOCAL_HOOK="$(git rev-parse --git-dir)/hooks/pre-push"
if [ -x "${LOCAL_HOOK}" ]; then
  exec "${LOCAL_HOOK}" "$@"
fi
